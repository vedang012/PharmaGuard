spring.application.name=pharmaguard

# Default to prod so DevVcfController (@Profile("dev")) is never registered.
# To enable dev endpoints: --spring.profiles.active=dev
spring.profiles.default=prod
server.port=${PORT:8080}
app.frontend-url=${FRONTEND_URL}


# ?? Google Gen AI (Gemini) ???????????????????????????????????????????????????
# Get your key at https://aistudio.google.com/app/apikey
google.ai.api-key=${GEMINI_API}
